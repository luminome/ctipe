{
  "name": "ctipe-runtime",
  "version": "1.0.0",
  "description": "A getter app for CTIPe data acquisition and storage at railway.com",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start-watch": "nodemon server/index.js --ignore './assets/*.json'",
    "dev": "concurrently --kill-others \"npm run start-watch\"",
    "start": "node ./server/index.js --omit=dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.0",
    "nodemon": "^2.0.20"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
